{
  "name": "amazon-tally-xml-utility",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node static-server.js 59231",
    "start": "node static-server.js 59231",
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html *.css *.js dist/"
  }
}
